# Hedera Token Service(HTS) Fungible Token Creator Example

To run this example:

- `npm install` or `yarn`
- `npm run dev` or `yarn dev`

## Hedera Documentation
[Token Service](https://docs.hedera.com/guides/docs/sdks/tokens)
